import 'package:flutter_test/flutter_test.dart';

import 'package:gatewise_app/app.dart';

void main() {
  test('GateWise app can be constructed', () {
    expect(const GateWiseApp(), isA<GateWiseApp>());
  });
}
